#include<iostream>
using namespace std;
 int main()
 {
     int i,arr[4],high=0;
     for (i=0;i<4;i++)
     {
        cin>> arr[i];
     }
     
    high= arr[0];
    for(i=0;i<4;i++)
    {
    if(high<arr[i])    
    {
        high=arr[i];
    }
    }
    cout<<high;
    
     
 }
